{
  "name": "aldwino",
  "private": true,
  "workspaces": [
    "app/web",
    "app/api"
  ],
  "scripts": {
    "dev": "bunx tauri dev",
    "dev:services": "bun run scripts/dev-services.ts",
    "build": "bunx tauri build",
    "typecheck": "bun run --cwd app/web typecheck && bun run --cwd app/api typecheck",
    "lint": "bun run --cwd app/web lint && bun run --cwd app/api lint"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@tauri-apps/cli": "^2",
    "eslint": "^9",
    "eslint-plugin-react-hooks": "^5",
    "globals": "^15",
    "typescript": "^5",
    "typescript-eslint": "^8"
  }
}
