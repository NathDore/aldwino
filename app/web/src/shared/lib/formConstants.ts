// Shared sizing constants for form elements, applied across all forms so
// equivalent fields (labels, text inputs, date pickers) share dimensions.

/** Font size for all form labels. */
export const LABEL_FONT_SIZE = "text-sm";

/** Font size for standard one-line and multi-line text inputs (not compact fields like time entry). */
export const TEXT_INPUT_FONT_SIZE = "text-base";

/** Width and height for one-line text inputs. */
export const ONE_LINE_TEXT_INPUT_WIDTH = "w-full";
export const ONE_LINE_TEXT_INPUT_HEIGHT = "h-[40px]";

/** Width and height for multi-line text inputs. */
export const MULTI_LINE_TEXT_INPUT_WIDTH = "w-full";
export const MULTI_LINE_TEXT_INPUT_HEIGHT = "h-20";

/** Width and height for date pickers. */
export const DATE_PICKER_WIDTH = "w-12";
export const DATE_PICKER_HEIGHT = "h-[38px]";

/** Width and height (in px) for form modals/popovers. */
export const MODAL_WIDTH = 1200;
export const MODAL_HEIGHT = 1000;
